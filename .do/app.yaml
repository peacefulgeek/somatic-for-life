name: somatic-for-life
region: nyc
services:
  - name: web
    github:
      repo: OWNER/somatic-for-life
      branch: main
      deploy_on_push: true
    build_command: pnpm install --frozen-lockfile && pnpm build
    run_command: NODE_ENV=production node scripts/start-with-cron.mjs
    http_port: 3000
    instance_count: 1
    instance_size_slug: basic-xxs
    health_check:
      http_path: /health
      initial_delay_seconds: 10
      period_seconds: 30
      timeout_seconds: 5
      success_threshold: 1
      failure_threshold: 3
    envs:
      - key: NODE_ENV
        value: production
        scope: RUN_AND_BUILD_TIME
      - key: PORT
        value: "3000"
        scope: RUN_AND_BUILD_TIME
      - key: AUTO_GEN_ENABLED
        value: "true"
        scope: RUN_TIME
      - key: DATABASE_URL
        scope: RUN_TIME
        type: SECRET
      - key: OPENAI_API_KEY
        scope: RUN_TIME
        type: SECRET
      - key: OPENAI_BASE_URL
        value: https://api.deepseek.com
        scope: RUN_TIME
      - key: OPENAI_MODEL
        value: deepseek-chat
        scope: RUN_TIME
      - key: GH_PAT
        scope: RUN_TIME
        type: SECRET
      - key: AMAZON_TAG
        value: spankyspinola-20
        scope: RUN_TIME
